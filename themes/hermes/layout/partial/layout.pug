doctype
html(lang=config.language)
    head
        include head
    body
        .wrap
            header
                a.logo-link(href=url_for())
                    p.site-title= config.title_short
                    //- img(src=url_for(config.logo) alt="logo")
                include nav
            main.container
                block container
            footer
                block pagination
                block copyright
        include scripts
